<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="/stylesheets/adminTemplate.css">
    <link rel="stylesheet" href="/stylesheets/adminhome.css">
    <link rel="stylesheet" href="/stylesheets/adminsidebar.css">
    <link rel="stylesheet" href="/stylesheets/adminManageEmployee.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
   
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>
<body>
                <%- include("sidebar.ejs") %> 
                
                <div class="template-screens">
                    <div class="pending-gigs">
                        <div class="mnge-container">
                            <div class="mnge-header">
                                <% if (gig.status == 'approved') { %>
                                    <form action="/admin/dashboard/pending-gig-approvals/disApprove/<%= gig.id %>" method="POST">
                                        <button type="submit">Reject</button>
                                    </form>    
                                <% } %>
                                <% if (gig.status == 'disapproved') { %>
                                    <form action="/admin/dashboard/pending-gig-approvals/approve/<%= gig.id %>" method="POST">
                                        <button type="submit">Approve</button>
                                    </form>       
                                <% } %>
                                <% if (gig.status == 'pending') { %>
                                    <form action="/admin/dashboard/pending-gig-approvals/approve/<%= gig.id %>" method="POST">
                                        <button type="submit">Approve</button>
                                    </form>   
                                    <form action="/admin/dashboard/pending-gig-approvals/disApprove/<%= gig.id %>" method="POST">
                                        <button type="submit">Reject</button>
                                    </form>      
                                <% } %>
                            
                            </div>
                            <div class="mnge-emp-table">
                                <div class="div">
                                    <p><%= gig.title %> </p><br>
                                
                                    <p><%= gig.description %> </p><br>
                                    <p><%= gig.id %> </p>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
           </div>
    </div>

    <script src="/javascript/admin.js"></script>
</body>
</html>